<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".MainActivity"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient">

    <!-- Admin Button in Top Right -->
    <Button
        android:id="@+id/main_admin_login_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Login as Admin"
        android:textStyle="bold"
        android:textSize="14sp"
        android:backgroundTint="#074887"
        android:textColor="#FFFFFF"
        android:padding="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="16dp"/>

    <!-- "Home Page" Title -->
    <TextView
        android:id="@+id/main_msg_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Home Page"
        android:textSize="30sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        app:layout_constraintTop_toBottomOf="@+id/main_admin_login_btn"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp" />

    <!-- App Logo -->
    <ImageView
        android:id="@+id/app_logo_img"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/cynance"
        android:contentDescription="App Logo"
        app:layout_constraintTop_toBottomOf="@id/main_msg_txt"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp" />

    <!-- Username Display -->
    <TextView
        android:id="@+id/main_username_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="{{username}}"
        android:textSize="30sp"
        android:textColor="#000000"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/app_logo_img"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

    <!-- Login Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/main_login_btn"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:text="Login"
        android:textSize="20sp"
        android:textStyle="bold"
        android:backgroundTint="#074887"
        android:textColor="#FFFFFF"
        app:layout_constraintTop_toBottomOf="@id/main_username_txt"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp" />

    <!-- Signup Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/main_signup_btn"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:text="Signup"
        android:textSize="20sp"
        android:textStyle="bold"
        android:backgroundTint="#074887"
        android:textColor="#FFFFFF"
        app:layout_constraintTop_toBottomOf="@id/main_login_btn"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
